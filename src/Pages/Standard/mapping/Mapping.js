import Container from "@material-ui/core/Container";
import React from "react";

const Mapping = props => {
    return (
        <Container>
            <p>Mapping</p>
        </Container>);
}

export default Mapping;
